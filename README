## How to Run the Project

```bash
docker compose build      # To install dependencies needed to start
docker compose up         # To start the server
# Use CTRL + C to stop the server
docker compose down       # To take down the containers
